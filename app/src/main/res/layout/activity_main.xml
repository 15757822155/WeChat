<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#fff"
    tools:context="com.hdsh.wechat.avtivity.MainActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary">
    </android.support.v7.widget.Toolbar>

    <!--<fragment-->
        <!--android:id="@+id/fragment_main"-->
        <!--android:name="com.hdsh.wechat.fragment.MessageFragment"-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="match_parent"-->
        <!--android:layout_above="@+id/content_navigation"-->
        <!--android:layout_below="@id/toolbar"></fragment>-->
    <android.support.v4.view.ViewPager
        android:id="@+id/viewpage_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/content_navigation"
        android:layout_below="@id/toolbar"></android.support.v4.view.ViewPager>

    <include
        android:id="@+id/content_navigation"
        layout="@layout/content_navigation_bottom"></include>
</RelativeLayout>
